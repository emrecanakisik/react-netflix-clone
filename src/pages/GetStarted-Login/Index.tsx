import React from "react";
import { Outlet } from "react-router-dom";

const GetStartedLoginLayout = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default GetStartedLoginLayout;
